import * as React from 'react'
import { StyledWrapper } from './styled'

const Component = (props) => {
    return <StyledWrapper size={props.size} src={props.src}></StyledWrapper>
}

export default Component
