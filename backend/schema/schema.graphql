scalar Upload

directive @auth on FIELD_DEFINITION

type Query {
    ping: String!
}

type Mutation {
    ping: String!
}

type PageInfo {
    total: Int
    last_page: Int
    current_page: Int
    per_page: Int
}

type R_Basic {
    code: Int!
    msg: String!
}
