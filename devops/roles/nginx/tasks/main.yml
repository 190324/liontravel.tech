- name: install nginx
  apt:
    name: nginx
    state: latest
  notify:
    - started nginx

- name: enable service nginx on reboot
  service:
    name: nginx
    enabled: yes
  notify:
    - restart nginx